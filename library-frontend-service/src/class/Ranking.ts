export default class Ranking {
  rank: string;

  name: string;

  num: number;

  constructor(rank: string, name: string, num:number) {
    this.rank = rank;
    this.name = name;
    this.num = num;
  }
}
